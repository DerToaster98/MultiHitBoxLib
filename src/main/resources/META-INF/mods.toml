modLoader="${mod_loader}"
loaderVersion="${loader_version}"
license="${mod_license}"
issueTrackerURL="${mod_issueTrackerURL}"

[[mods]]
modId="${modid}"
version="${mod_version}"
displayName="${mod_name}"
displayURL="https://www.curseforge.com/minecraft/mc-mods/multihitboxlib"
logoFile="logo.png"
credits="${mod_credits}"
description='''${mod_description}'''

# Base
[[dependencies."${modid}"]]
    modId="forge"
    mandatory=true
    versionRange="${forge_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies."${modid}"]]
    modId="minecraft"
    mandatory=true
    versionRange="${mc_version_range}"
    ordering="NONE"
    side="BOTH"

# Dependencies (Optional)
#   COMMON
[[dependencies."${modid}"]]
    modId="geckolib"
    mandatory=false
    versionRange="${geckolib_version_range}"
    ordering="BEFORE"
    side="BOTH"

[[dependencies."${modid}"]]
    modId="azurelib"
    mandatory=false
    versionRange="${azurelib_version_range}"
    ordering="BEFORE"
    side="BOTH"

[[dependencies."${modid}"]]
    modId="jei"
    mandatory=false
    versionRange="${jei_version_range}"
    ordering="BEFORE"
    side="BOTH"

[[dependencies."${modid}"]]
    modId="jeed"
    mandatory=false
    versionRange="${jeed_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies."${modid}"]]
    modId="jade"
    mandatory=false
    versionRange="${jade_version_range}"
    ordering="NONE"
    side="BOTH"

#   CLIENT
[[dependencies."${modid}"]]
    modId="appleskin"
    mandatory=false
    versionRange="${appleskin_version_range}"
    ordering="NONE"
    side="CLIENT"

[[dependencies."${modid}"]]
    modId="catalogue"
    mandatory=false
    versionRange="${catalogue_version_range}"
    ordering="NONE"
    side="CLIENT"

[[dependencies."${modid}"]]
    modId="gamemenumodoption"
    mandatory=false
    versionRange="${gmmo_version_range}"
    ordering="NONE"
    side="CLIENT"

[[dependencies."${modid}"]]
    modId="mixinbooster"
    mandatory=true
    versionRange="${mixinbooster_version_range}"
    ordering="BEFORE"
    side="BOTH"